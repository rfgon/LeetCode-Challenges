#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <vector>

namespace utils
{

    class Solution {
        public:
            std::vector<int> twoSum(std::vector<int>& nums, int target) {
                std::vector<int> solution(2);

                return solution;
            }
    };

} // namespace utils

#endif // UTILS_H
